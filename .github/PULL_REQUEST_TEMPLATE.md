## Contexte

Décrivez brièvement **pourquoi** ce changement existe (bug rencontré, fonctionnalité
demandée, amélioration).

## Changements

- [ ] Liste des changements apportés
- [ ] (optionnel) captures d'écran si l'UI change

## Vérifications

- [ ] `make test` passe (tous les tests verts)
- [ ] `make lint` passe (ruff, 100 colonnes)
- [ ] `make format` appliqué
- [ ] Les checks CI (`quality`, `tests-windows`, `tests-macos`) sont verts

## Notes

Aucun secret, artefact (`dist/`, `build/`) ou fichier personnel n'est inclus dans cette PR.